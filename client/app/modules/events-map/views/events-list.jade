table.table( ng-show='events.length' )
  thead
    tr
      th Source
      th Count
  tbody
    tr( ng-repeat='event in events track by event.eventId'
      ng-click='showEvent(event)' )
      td
        img( ng-src='{{ event.eventSource | sourceIcon }}' )
      td {{ event.weight }}
